import XCTest
@testable import ProfileShotView

final class ProfileShotViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ProfileShotView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
